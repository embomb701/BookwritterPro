# Optional dependencies for the HTTP API server and the MCP server.
# The core package and its CLI need none of these (see requirements.txt).
#
#   pip install -r requirements-server.txt
#
# Run the web app:  python -m bookwriter.serve   (or: bookwriter-serve)
fastapi>=0.110
uvicorn[standard]>=0.27
httpx>=0.27

# Only needed if you expose BookwriterPro as an MCP server.
mcp>=1.2
